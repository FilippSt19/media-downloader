services:
  backend:
    build:
      context: ./backend

    container_name: media-downloader-backend

    ports:
      - "4000:4000"

    volumes:
      - ./backend:/app
      - /app/node_modules

    working_dir: /app

    command: npm run dev

    environment:
      PORT: 4000