services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile

    container_name: media-downloader-backend

    ports:
      - "4000:4000"

    volumes:
      - ./backend/src:/app/src

    environment:
      PORT: 4000